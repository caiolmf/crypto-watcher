import React from 'react';
import { LoaderContainer } from './styledComponents';

const Loader = () => {
  return (
    <LoaderContainer>
      <h1>Loading</h1>
    </LoaderContainer>
  );
};

export default Loader;
